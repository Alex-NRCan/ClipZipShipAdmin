@echo off
@sudo docker rm -f $(sudo docker ps -a -q)

cd /home/alexroy/docker
sudo aws s3 cp s3://quality-bucket/docker-images/quality-web /home/alexroy/docker/quality-web
sudo docker load -i quality-web
sudo docker run -d -p 80:80 --restart always quality-web



@for i in {0..20}; do (curl -Is http://localhost:5000/api/ui/ | head -n1 &); done;
